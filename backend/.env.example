DB_HOST=localhost
DB_PORT=5432
DB_USER=medinaa
DB_PASSWORD=medinaa
DB_NAME=medinaa

NODE_ENV=development
PORT=3000

# Secret de signature JWT — OBLIGATOIRE (le backend refuse de démarrer sans).
# Générer une valeur forte : openssl rand -base64 48
JWT_SECRET=

# Optionnel — enrichissement Google Places (scripts/google-enrich.cjs). Places API + facturation.
GOOGLE_MAPS_API_KEY=
